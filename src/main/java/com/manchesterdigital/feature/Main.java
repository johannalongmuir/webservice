package com.manchesterdigital.feature;

public class Main {

    public static void main(String[] args) {
        // Features one and two
        /*Feature featureOne = new Feature("One", false);
        Feature featureTwo = new Feature("Two", false);
        FeatureHandler featureHandler = new FeatureHandler();
        // I want to see Map printed. Expect it to be empty initially
        featureHandler.printMap();

        featureHandler.addFeatures(featureOne);
        // Added feature one to Map. Expect Map to print key One
        featureHandler.printMap();

        featureHandler.addFeatures(featureTwo);
        // Added feature two to Map. Expect Map to print key One and Two
        featureHandler.printMap();

        featureHandler.addFeatures(featureOne);
        // Adding feature one again. Expect it to still be key One and Two and not double up
        featureHandler.printMap();

        featureHandler.addFeatures(featureTwo);
        // When using addFeatures on featureTwo after in map, changes Boolean value to false again but not featureOne
        featureHandler.printMap();

        featureTwo.setIsEnabled(true);
        featureHandler.addFeatures(featureTwo);
        // When using featureTwo setIsEnabledUpdated, changes Boolean value to true but not featureOne
        featureHandler.printMap();

        */

    }



}
